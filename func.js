var x = myFunction(4, 3);   // Function is called, return value will end up in x -test function

function myFunction(a, b) {
  return a * b;             // Function returns the product of a and b testing
}